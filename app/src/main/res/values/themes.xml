<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PoseExercise" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimaryContainer">@color/light_blue</item>
        <item name="android:textColorSecondary">@color/black</item>
        <item name="textAppearanceButton">@color/white</item>
        <item name="android:titleTextAppearance">@color/texttitlelight</item>
        <item name="android:subtitleTextAppearance">@color/textsubtitlelight</item>

        <item name="background">@drawable/bg3</item>
        <item name="logo">@drawable/formfixlogoblk</item>

        <item name="textInputStyle">@color/dayinput</item>

        <item name="iconTint">@color/black</item>

    </style>

    <style name="Theme.PoseExercise" parent="Base.Theme.PoseExercise" />
</resources>