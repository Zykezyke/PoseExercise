<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PoseExercise" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/white</item>
        <item name="colorPrimaryContainer">@color/dark_light_blue</item>
        <item name="textAppearanceButton">@color/white</item>

        <item name="android:titleTextAppearance">@color/texttitledark</item>
        <item name="android:subtitleTextAppearance">@color/textsubtitledark</item>
        <item name="background">@drawable/bg2</item>
        <item name="logo">@drawable/formfixlogowht</item>

        <item name="textInputStyle">@color/nightinput</item>
    </style>
</resources>